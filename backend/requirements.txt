# Exclude fastapi 0.136.3: security advisory MAL-2026-4750 flags that release
# for adding an undocumented package to its optional "standard" extra. Drop the
# exclusion once a fixed release is published.
#
# Cap below 0.137: that release reworked ``include_router`` to defer route
# inclusion behind lazy ``_IncludedRouter`` wrappers rather than flattening the
# included APIRoutes onto ``app.routes`` eagerly. The app still serves and its
# OpenAPI schema is unchanged, but route-table introspection (the ETag
# route-coverage check) and any code that walks ``app.routes`` expecting
# APIRoutes no longer sees them. Pin to the eager model the backend is built
# against until the introspection is migrated to the new model deliberately.
fastapi>=0.115.0,!=0.136.3,<0.137
uvicorn[standard]>=0.32.0
pydantic>=2.0
python-dotenv>=1.0

pynput>=1.7.0

# Screen reader
numpy>=1.20.0
opencv-python-headless>=4.9.0
mss>=9.0.0
# onnxruntime + onnxruntime-directml share the same `onnxruntime/` package dir;
# whichever was installed last wins at import time. openocr-python (below)
# hard-depends on plain `onnxruntime`, so a fresh `pip install -r` ends up with
# plain `onnxruntime` shadowing directml. To restore DirectML provider visibility
# on Windows, run after install:
#     pip install --force-reinstall --no-deps onnxruntime-directml
# `backend/build_app.py` performs this automatically before PyInstaller-freeze.
# The shadow pair must also stay VERSION-COHERENT: directml releases lag the
# plain package, and a newer plain `__init__` over the older directml capi
# fails at import (`OrtDeviceVendorId`, plain 1.26.0 over directml 1.24.4), so
# the Windows shadow pins to the directml release line.
onnxruntime-directml==1.24.4; platform_system == "Windows"
onnxruntime==1.24.4; platform_system == "Windows"
onnxruntime; platform_system != "Windows"
rapidfuzz>=3.0

# Local OCR for skill / profession panel scans
openocr-python>=0.1.5
